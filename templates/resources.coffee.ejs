<%
var filesArrayAsStr = _(files).filter(function (it) {
    return fs.lstatSync(it).isFile()
}).reject(function (it) {
    return it.includes('/.') || it.endsWith('.js');
}).map(function (it) {
    return '"' + it + '"';
}).join(',');

filesArrayAsStr = '[' + filesArrayAsStr + ']';
%>

<%- "# Auto generated. Don't edit" %>
module.exports = <%- filesArrayAsStr %>
